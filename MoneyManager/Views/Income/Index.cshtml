@model IEnumerable<MoneyManager.Entities.IncomeEntity>

@{
    ViewData["Title"] = "Przychody";
}

<h2>Lista przychodów</h2>

<form method="GET" class="form-inline mb-2">
    <input type="text" name="name" placeholder="Filtruj po nazwie" class="form-control" />
    <button type="submit" class="btn btn-primary ml-2">Szukaj</button>
</form>

<a class="btn btn-primary" asp-area="" asp-controller="Income" asp-action="Add">Dodaj przychód</a>

<table class="table">
    <thead>
        <tr>
            <th>Nazwa</th>
            <th>Kwota</th>
            <th>Data</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name </td>
                <td>@item.Amount zł</td>
                <td>@item.IncomeDate.ToString("yyyy/MM/dd")</td>
                <td>
                    <a class="btn btn-default" asp-controller="Income" asp-action="Edit" asp-route-id="@item.Id">Edytuj</a>
                    <a class="btn btn-danger" asp-controller="Income" asp-action="Delete" asp-route-id="@item.Id">Usuń</a>
                </td>
            </tr>
        }
    </tbody>
</table>
